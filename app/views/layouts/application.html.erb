<!DOCTYPE html>
<html class="w-full">
  <head>
    <title><%= content_for(:title) || "Miniplan" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "https://www.nerdfonts.com/assets/css/webfont.css",
    rel: "stylesheet",
    media: "all" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <main class="w-full flex flex-col items-center h-full">
      <!-- Flash Messages -->
      <% if flash.any? %>
        <div class="fixed top-4 right-4 z-50 space-y-2" data-controller="flash-messages">
          <% flash.each do |type, message| %>
            <div class="flash-message bg-white border-l-4 <%= type == 'notice' ? 'border-green-500' : type == 'alert' ? 'border-red-500' : 'border-blue-500' %> rounded-lg shadow-lg p-4 max-w-sm opacity-100 transition-all duration-300 transform translate-x-0"
                 data-flash-messages-target="message"
                 data-flash-type="<%= type %>">
              <div class="flex items-center">
                <% if type == 'notice' %>
                  <i class="nf nf-fa-check_circle text-green-500 mr-3 text-lg"></i>
                <% elsif type == 'alert' %>
                  <i class="nf nf-fa-exclamation_triangle text-red-500 mr-3 text-lg"></i>
                <% else %>
                  <i class="nf nf-fa-info_circle text-blue-500 mr-3 text-lg"></i>
                <% end %>
                <span class="text-gray-800 font-medium flex-1"><%= message %></span>
                <button class="ml-3 text-gray-400 hover:text-gray-600 transition-colors duration-200"
                        data-action="click->flash-messages#dismiss"
                        data-flash-messages-target="closeButton">
                  <i class="nf nf-fa-times"></i>
                </button>
              </div>
            </div>
          <% end %>
        </div>
      <% end %>
      
      <%= yield %>
    </main>
  </body>
</html>